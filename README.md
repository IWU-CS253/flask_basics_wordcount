# flask_basics_wordcount
Create a basic Flask app that:
- takes a string from the user
- displays the word count, the character count, and the average word length
- OPTIONAL: displays the string backwards
